export const ucfirst = (str) => str.charAt(0).toUpperCase() + str.slice(1);

export const toastProps = {
  variant: "solid",
  status: "success",
  isClosable: true,
  duration: 3000,
  position: "top",
};
